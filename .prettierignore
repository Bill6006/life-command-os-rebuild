dist
coverage
node_modules
playwright-report
test-results
package-lock.json
# Copied verbatim from the owner, and reformatting it would make the copy a
# paraphrase (section 34, D-079).
docs/CANONICAL_REBUILD_PLAN.md

# Written by the independent QA conversation, which the builder may not edit
# (D-077, qa/README.md). Holding somebody else's document to this repository's
# formatter is a rule that breaks at every QA handoff and can only be satisfied
# by the one person forbidden to satisfy it — CI failed on exactly that.
docs/qa/PHASE_*_QA_HANDOFF.md

# The independent sealed owner-use review, for both of the reasons above at once.
# It was written by an independent Codex conversation the builder may not edit,
# and the owner's instruction when it was handed over was that it must not be
# deleted, replaced, formatted or otherwise altered. Running the formatter over
# it would turn 36 sealed browser evidence entries and 44 findings into a
# paraphrase of themselves, which is the same objection that already exempts the
# canonical plan. It is committed byte-for-byte as it was received.
docs/qa/WHOLE_APP_OWNER_USE_REVIEW.md

# The deferred instrument findings, preserved verbatim by owner decision D-210.
# The bounded closeout is written in terms that this file may not be edited,
# removed, renumbered or reordered, and that every QA-84-0xx identifier in it
# must still resolve. Running the formatter over it would be an edit, for the
# same reason the QA handoffs above are exempt: it is another conversation’s
# document, preserved as it was received.
docs/qa/INSTRUMENT_HARDENING_BACKLOG.md
