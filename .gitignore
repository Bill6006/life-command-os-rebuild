node_modules
dist
dist-ssr
coverage
playwright-report
test-results
*.local
# Scratch harnesses run from the repo root because they need its
# node_modules — the Android gate is one. They are not part of the product.
*.local.mjs
.DS_Store
.claude
.vscode/*
!.vscode/extensions.json
.idea

# Canonical plan section 39 — real owner data must never enter repo history.
# Synthetic fixtures live in tests/fixtures and are committed intentionally.
*.backup.json
*-export-*.json
private/
owner-data/
